export interface Recipes {
  recipe: string;
  quantity: string;
  unit: string;
  unitConvert: string;
  convert: string | undefined;
}

export interface alertType {
  message: string;
  status: boolean;
}
